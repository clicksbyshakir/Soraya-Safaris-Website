---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

type Props = {
  eyebrow?: string;
  title: string;
  body: string;
  note?: string;
  image: ImageMetadata;
  alt: string;
  compact?: boolean;
};

const { eyebrow, title, body, note, image, alt, compact = false } = Astro.props;
---

<section class:list={["hero", compact && "hero--compact"]} aria-label={eyebrow ?? title}>
  <Image
    class="hero__image"
    src={image}
    alt={alt}
    widths={[640, 960, 1280, 1600, 2000, 2400]}
    sizes="100vw"
    quality={62}
    loading="eager"
    fetchpriority="high"
    decoding="async"
  />
  <div class="hero__content">
    <div class="hero__content-inner">
      {eyebrow && <p class="eyebrow">{eyebrow}</p>}
      <h1 class="hero__title">{title}</h1>
      <p class="hero__subhead">{body}</p>
      {note && <p class="hero__note">{note}</p>}
    </div>
  </div>
</section>
