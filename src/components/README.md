# Components

Reusable Astro components live here.

Naming conventions:

- Use PascalCase filenames, for example `SiteHeader.astro`.
- Keep presentation components free of page-specific copy.
- Put typed content in content collections, data files, or page frontmatter rather than inside shared visual components.
