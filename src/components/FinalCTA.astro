---
type Props = {
  eyebrow: string;
  title: string;
  body: string;
  cta?: string;
};

const { eyebrow, title, body, cta = "Start a conversation" } = Astro.props;
---

<section class="section final-cta">
  <div class="container final-cta__inner">
    <div>
      <p class="eyebrow">{eyebrow}</p>
      <h2>{title}</h2>
      <p>{body}</p>
    </div>
    <a class="button button--primary" href="/contact">{cta}</a>
  </div>
</section>
